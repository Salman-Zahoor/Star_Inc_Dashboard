export const ROLES = Object.freeze({
    admin: 'admin',
    manager: 'manager',
});

export const SUB_ROLES = Object.freeze({
    // inspector: 'inspector',
    // driver: 'driver',
    // cleaner: 'cleaner',
});
